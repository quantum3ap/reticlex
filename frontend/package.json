{
  "name": "reticlex-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ReticleX user interface: rendering, state and localization.",
  "scripts": {
    "test": "node --test tests/",
    "serve": "node ../scripts/dev-server.js"
  },
  "engines": { "node": ">=20" }
}
